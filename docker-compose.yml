services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: ozon_saas
      POSTGRES_USER: ozon
      POSTGRES_PASSWORD: ozon
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: